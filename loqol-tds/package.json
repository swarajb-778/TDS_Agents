{
  "name": "loqol-tds",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "California TDS seller disclosure — question registry, flow engine, and DocuSeal mapping",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "validate": "tsx scripts/validate.ts",
    "smoke": "tsx scripts/smoke.ts",
    "check": "npm run typecheck && npm run validate && npm run smoke"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
